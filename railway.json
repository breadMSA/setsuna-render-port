{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y python3 python3-pip python3-venv python3-full python-is-python3 && npm install && python3 -m venv .venv && .venv/bin/pip install -r python_requirements.txt"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "variables": {
    "PATH": "/app/.venv/bin:${PATH}",
    "PYTHONPATH": "/app"
  }
}